# Life Stations — Agent QA Proof

**UTC:** `2026-07-23T18:09:58Z`  
**Base commit:** `abe95a9` (working tree includes QA fixes below)  
**App version:** `3.0.66+70`  
**Project:** `aaaa-4eee0`  
**Executor:** Cursor Agent (Comprehensive A–Z Agent QA Plan)

---

## Go / No-Go

| Track | Recommendation |
|-------|----------------|
| **Automated / agent gates** | **GO** — static, full suite (248), backend prove, web + AAB builds, hosting HTTP 200 |
| **Play Internal + Hosting ship of this working tree** | **GO with conditions** — commit + deploy hosting after human Phase 7 smoke; Play Internal preferred over sideload |
| **Production risk-accept** | Human Phase 7 still **HUMAN_REQUIRED** (SMS, two-account Match, Play Integrity) |

---

## Gate results

| ID | Gate | Result | Evidence |
|----|------|--------|----------|
| S1 | `dart format --set-exit-if-changed lib test` | **PASS** | 0 changed after format pass |
| S2 | `flutter analyze` | **PASS** | No issues found |
| S3 | `node --check firebase/functions/index.js` | **PASS** | Exit 0 |
| P2 | `flutter test --concurrency=1` | **PASS** | **248** tests, 0 failures (`/tmp/flutter_test_final.log`) |
| B2 | `npm run prove:backend` | **PASS** | After script sync to Functions-only `rate_limits` |
| B1/B4 | Security + privacy contracts | **PASS** | `firebase_rules_proof_test`, `security_checklist_contract_test`, `privacy_contract_test` |
| B5 | Security doc sync | **PASS** | Throttle App Check claims updated to auth-capped |
| R1 | Web release build | **PASS** | `build/web` OK |
| R2 | Sync `firebase/public` | **PASS** | `index.html` + `.well-known/assetlinks.json` |
| R3 | AAB release | **PASS** | `build/app/outputs/bundle/release/app-release.aab` (~75 MB) |
| R4 | Hosting smoke | **PASS** | `curl -sI https://aaaa-4eee0.web.app` → HTTP/2 200 |
| Deploy | Firebase deploy | **NOT RUN** | Per plan — ask separately |

---

## Phase 0 gap map (summary)

| Bug / area | Status after QA |
|------------|-----------------|
| Name vanishes; photo/posts survive | Covered — `account_identity_refresh_test` |
| Web Save App Check / Play copy | Covered — bypass classifier + no “Install from Play” |
| Sign out extra subtitle | Covered — asserts absence of posts/phone restore copy |
| Kuwait Jobs multi-position | Covered — domain + picker + save wiring |
| Match / like-back / unlock | Covered — existing likes/match/privacy packs |
| Rooms/Bikes/Home Help save | Covered — `domain_save_wiring_test` + all-domain grid switch |
| Security doc drift (throttle App Check) | Fixed |

See also [`docs/qa-proof/_PHASE0_GAP_MAP.md`](_PHASE0_GAP_MAP.md).

---

## Defects found & fixed this run

| Severity | Issue | Fix |
|----------|-------|-----|
| **P0** | Switching domains disposed shared `DiscoveryStore` via `ChangeNotifierProxyProvider` (feed crash after multi-switch) | [`lib/main.dart`](../../lib/main.dart) — `ListenableProxyProvider` with no-op `dispose` |
| P1 | `contact_share_choice_test` failed without live Auth phone | `debugHasLivePhoneAuth` test hook in [`otp_sheet.dart`](../../lib/widgets/onboarding/otp_sheet.dart) |
| P1 | `widget_smoke_test` expected removed “Optional profile” | Updated Me assertions + Sign out in Settings |
| P2 | `prove:backend` expected legacy `hits <= 10` client rate_limits | Script asserts Functions-only deny writes |

---

## Tests added / extended

- App Check infrastructure classification unit test (`media_trust_throttle_test`)
- Settings Sign out: no “Posts stay…” / “Verify again” copy
- Kuwait Jobs Save payload in `domain_save_wiring_test`
- UI matrix **C2b**: every enabled domain switchable without disposing stores
- Contact share test uses live-phone override
- Widget smoke aligned to current Me / Settings UI

---

## Human checklist (Phase 7) — not agent-PASS

Mark each when done on a real device/browser:

1. [ ] Incognito web: verify phone → Account Save name → refresh → name remains  
2. [ ] Incognito web: Kuwait Jobs photo → Save → no App Check / Play message; post on Me  
3. [ ] Sign out → local Account cleared → re-verify same phone → posts restore  
4. [ ] Play Internal AAB: post + like + Match unlock  
5. [ ] Two accounts: like → accept → Match → WhatsApp/Telegram open with prefill  
6. [ ] Multi-tab: phone verify on tab B does not wipe name on tab A after refresh  

---

## Open / accepted risks

| Item | Status |
|------|--------|
| App Check on Firestore/Storage | OPEN (auth-only rules) |
| Throttle callables without `enforceAppCheck` | **ACCEPTED** for web/sideload; unlock/delete still enforced |
| Live emulator abuse B1–B15 / bot flood C* | NOT RUN (schedule staging) |
| Hosting not redeployed with domain-switch fix | Working tree has fix; **deploy when you ask** |

---

## Suggested next commands (you)

```bash
# After review, commit QA tree then optionally:
cd firebase && firebase deploy --only hosting --project aaaa-4eee0
# Upload AAB to Play Internal testing
```

---

*End of agent proof. Human Phase 7 required before claiming full production verification.*
